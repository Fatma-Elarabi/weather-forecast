import { ConvertToImgPipe } from './convert-to-img.pipe';

describe('ConvertToImgPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToImgPipe();
    expect(pipe).toBeTruthy();
  });
});
